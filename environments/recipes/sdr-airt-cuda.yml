# the CPU computation requirements plus CUDA (GPU compute) and the proprietary AirStack SDR drivers

# CPU computations (no GPU and no SDR hardware support)

layers:
  - layers/base.yml
  - layers/add-airt.yml